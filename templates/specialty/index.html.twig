{% extends 'base.html.twig' %}

{% block title %}Specialty index{% endblock %}

{% block body %}
    <h1>Specialty index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for specialty in specialties %}
            <tr>
                <td>{{ specialty.id }}</td>
                <td>{{ specialty.description }}</td>
                <td>
                    <a class="btn btn-success" href="{{ path('specialty_show', {'id': specialty.id}) }}">show</a>
                    <a class="btn btn-info" href="{{ path('specialty_edit', {'id': specialty.id}) }}">edit</a>
                    <a class="btn btn-warning" target='_blank' href="{{ path('specialty_report', {'id': specialty.id}) }}">report</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('specialty_new') }}">Create new</a>
{% endblock %}