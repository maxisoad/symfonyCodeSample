{% extends 'base.html.twig' %}

{% block title %}Specialty{% endblock %}

{% block body %}
    <h1>Specialty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialty.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ specialty.description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info left" href="{{ path('specialty_edit', {'id': specialty.id}) }}">edit</a>

    {{ include('specialty/_delete_form.html.twig') }}

    <a href="{{ path('specialty_index') }}">back to list</a>
{% endblock %}
